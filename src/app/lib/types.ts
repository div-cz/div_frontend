export type imageSizeEnum = 'small' | 'medium';

export type BookProps = {
  id: number;
  image: string;
  title: string;
  starRating: string;
  rating: string;
  year: string;
  genre: string;
  runtime: string;
  cast: string;
};
